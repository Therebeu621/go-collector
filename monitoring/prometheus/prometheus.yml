global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: collector_container
    static_configs:
      - targets:
          - collector:9090

  - job_name: collector_host
    static_configs:
      - targets:
          - host.docker.internal:9090
